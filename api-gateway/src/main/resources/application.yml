server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/auth/**

        - id: matchmaking-service
          uri: http://localhost:8082
          predicates:
            - Path=/queue/**

        - id: lobby-service
          uri: http://localhost:8083
          predicates:
            - Path=/lobby/**